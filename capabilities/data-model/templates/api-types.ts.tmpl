/**
 * Auto-generated by data-model capability v0.2.
 * Typed client + types for the {{entity}} entity. Regenerate via re-materialization.
 */

export interface {{entity}} {
  id: number;
  user_id: string;
  created_at: string | null;
  updated_at: string | null;
{{fieldsTypeScriptInterface}}
}

export interface {{entity}}CreatePayload {
{{fieldsTypeScriptCreate}}
}

const BASE = "/api/{{entitySnakePlural}}";

export async function list{{EntityPlural}}(): Promise<{{entity}}[]> {
  const res = await fetch(BASE, { credentials: "include" });
  if (!res.ok) throw new Error(`list{{EntityPlural}} failed: ${res.status}`);
  return res.json();
}

export async function get{{entity}}(id: number): Promise<{{entity}}> {
  const res = await fetch(`${BASE}/${id}`, { credentials: "include" });
  if (!res.ok) throw new Error(`get{{entity}}(${id}) failed: ${res.status}`);
  return res.json();
}

export async function create{{entity}}(
  payload: {{entity}}CreatePayload,
): Promise<{{entity}}> {
  const res = await fetch(BASE, {
    method: "POST",
    credentials: "include",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(payload),
  });
  if (!res.ok) throw new Error(`create{{entity}} failed: ${res.status}`);
  return res.json();
}

export async function delete{{entity}}(id: number): Promise<void> {
  const res = await fetch(`${BASE}/${id}`, {
    method: "DELETE",
    credentials: "include",
  });
  if (!res.ok) throw new Error(`delete{{entity}}(${id}) failed: ${res.status}`);
}
