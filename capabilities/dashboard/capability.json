{
  "id": "dashboard",
  "name": "Dashboard",
  "description": "Widget-first dashboard surface with Apple HIG 3-size widgets (170×170 / 360×170 / 360×360). The base seed ships with the widget runtime + a default Dashboard page; this capability is the contract that says 'this app uses the dashboard surface'.",
  "version": "0.1.0",
  "dependsOn": [],
  "requires": {
    "entities": "optional"
  },
  "configSchema": [
    {
      "key": "defaultWidgetSize",
      "type": "string",
      "required": false,
      "default": "medium",
      "enum": ["small", "medium", "large"],
      "description": "Initial widget size shown on the dashboard."
    }
  ],
  "wireUp": "wire-up.json",
  "intentHints": [
    "dashboard",
    "widget",
    "overview",
    "summary",
    "at a glance",
    "home screen"
  ]
}
