"""
App-specific API routes.

Each module in this package exposes a module-level `router = APIRouter()`.
backend/main.py auto-includes every router found here on startup (mirroring the
agent/workflow/trigger auto-discovery). Generated apps REPLACE backend/routes/app.py
with their own data endpoints; this package layout never changes.
"""
