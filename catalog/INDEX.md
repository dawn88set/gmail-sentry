# Catalog Index

_Auto-generated by `scripts/dump-catalog-index.mjs`. Do not edit by hand._

Generated: 2026-06-23T03:20:49.244Z

These integrations are **built in** — the Claritty platform manages OAuth/credentials per user. To use one: declare it in `intelligence.yaml#integrations`, list its tools in the agent, and call them (or `ctx.integration("<id>")`). No API keys, no OAuth code; test locally with `CLARITTY_FAKE_CREDS_<ID>`. See `INTEGRATIONS.md`. A service NOT listed here is not built in — use a custom tool or a clearly-labeled seed, never a faked one.

## Integrations

- **`airtable`** _data_ · integration · auth=apiKey — Read and write records in Airtable bases.
- **`anthropic`** _ai_ · integration · auth=apiKey — Call Anthropic Messages directly. Most apps use the Claritty proxy instead and get this for free.
- **`asana`** _productivity_ · integration · auth=oauth2 — Create and list tasks in Asana projects.
- **`brave-search`** _productivity_ · integration · auth=apiKey — Search the web with Brave Search to ground content in current results. Uses an API key (X-Subscription-Token).
- **`calendly`** _calendar_ · integration · auth=apiKey — List scheduled meetings and cancel events with a Calendly personal access token.
- **`clickup`** _productivity_ · integration · auth=apiKey — Create and list tasks in ClickUp.
- **`discord`** _communication_ · integration · auth=apiKey — Send messages to Discord channels and list guilds.
- **`dropbox`** _files_ · integration · auth=oauth2 — List and upload files in Dropbox.
- **`github`** _dev_ · integration · auth=oauth2 — List + create issues, open PRs, and search code in GitHub repos.
- **`gitlab`** _dev_ · integration · auth=apiKey — List issues and create merge requests in GitLab.
- **`gmail`** _email_ · integration · auth=oauth2 — Send and read email through the authenticated user's Gmail account.
- **`google-calendar`** _calendar_ · integration · auth=oauth2 — Read events and create events on the user's Google Calendar.
- **`google-drive`** _files_ · integration · auth=oauth2 — Search and list files in the user's Google Drive.
- **`hubspot`** _crm_ · integration · auth=oauth2 — Create and search contacts, and update deals in the user's HubSpot CRM.
- **`jira`** _productivity_ · integration · auth=oauth2 — Create and transition issues in Jira via the Atlassian OAuth.
- **`linear`** _productivity_ · integration · auth=oauth2 — Create issues, search, and comment in the user's Linear workspace.
- **`linkedin`** _social_ · integration · auth=oauth2 — Read the authenticated user's LinkedIn feed and publish posts to their personal feed or a Company Page they administer.
- **`mailchimp`** _marketing_ · integration · auth=apiKey — Add subscribers and send campaigns.
- **`microsoft-teams`** _communication_ · integration · auth=oauth2 — Post messages to Teams channels and chats.
- **`notion`** _productivity_ · integration · auth=oauth2 — Search, read, create, and update pages in the user's Notion workspace.
- **`openai`** _ai_ · integration · auth=apiKey — Call OpenAI chat completions, embeddings, and image generation. Routed via the Claritty proxy when no key is configured.
- **`outlook`** _email_ · integration · auth=oauth2 — Send and read email through the authenticated user's Outlook (Microsoft 365) account.
- **`outlook-calendar`** _calendar_ · integration · auth=oauth2 — Read and write events on the user's Outlook calendar.
- **`pipedrive`** _crm_ · integration · auth=apiKey — List and create deals in Pipedrive.
- **`posthog`** _analytics_ · integration · auth=apiKey — Capture events and run HogQL queries against your PostHog project.
- **`salesforce`** _crm_ · integration · auth=oauth2 — Run SOQL, create leads, and update opportunities in Salesforce.
- **`slack`** _communication_ · integration · auth=oauth2 — Post messages and read conversations across the authenticated user's Slack workspace.
- **`stripe`** _payments_ · integration · auth=apiKey — Create customers, list charges, and generate payment links.
- **`trello`** _productivity_ · integration · auth=apiKey — Create cards and list cards across Trello boards.
- **`twilio`** _communication_ · integration · auth=apiKey — Send SMS and place voice calls.
- **`x-twitter`** _social_ · integration · auth=oauth2 — Post tweets and search recent tweets.

## Tools (catalog)

- **`llm.classify`** (standalone) — Classify a piece of text into exactly one of the provided labels using the LLM proxy. The chosen label is snapped to one of the options so workflow branching is safe.
- **`llm.draft`** (standalone) — Draft a short message (email reply, post, note, summary blurb) from context in a given tone, using the LLM proxy. Returns text only — pair with a human-in-the-loop approval step before sending anything.
- **`llm.extract`** (standalone) — Extract structured fields from free text into a JSON object using the LLM proxy. Pass the field names (and optional descriptions) you want pulled out.
- **`llm.rank`** (standalone) — Rank/prioritize a list of items against a stated criterion using the LLM proxy. Returns the items ordered most-important first, each with a score and a one-line reason.
- **`llm.summarize`** (standalone) — Summarize a list of texts into a short digest using the LLM proxy.

## Agents (catalog)

- **`linkedin-summarizer`** — Fetch the user's LinkedIn feed, summarize it, and return the digest.
  Default tools: llm.summarize
  Default integrations: linkedin
- **`triage-agent`** — Triage a list of items: classify each into the given categories, prioritize by urgency/importance, and return the sorted, labeled list with the top items surfaced.
  Default tools: llm.classify, llm.rank

## Triggers

- **`schedule-daily`** _trigger_ — Fire a workflow once a day at a user-chosen time.

## Workflows (templates)

- **`morning-digest`** — Fire once per morning; run the LinkedIn summarizer agent and write the digest.

## Skills (vetted agent procedures)

- **`classify-and-triage`** — Classify each item into the given categories and rank by importance then urgency, with a grounded one-line reason per item, so the highest-leverage work surfaces first.
  Fits agents that: llm.classify, llm.rank
- **`draft-on-brand-reply`** — Draft a reply that matches the brand voice, leads with the answer, is concrete and safe to send (no commitments it wasn't given, no sensitive data).
  Fits agents that: llm.draft, gmail.send, slack.send, outlook.send
- **`summarize-with-citations`** — Produce a faithful, grounded summary that cites its sources, separates fact from inference, preserves the source's weighting, and never invents details.
  Fits agents that: llm.summarize, llm.extract
